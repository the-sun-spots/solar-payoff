// Declare estimateView object literal
// All functions for the 'EstimateView' object.

var estimateView = {};

estimateView.renderEstimate = function() {  // Render all calculations for final estimate view
};

estimateView.renderEstimateTab = function() {  // Render estimate values in lower portion of screen
};

estimateView.renderTaxIncentives = function() {  // render ALL 'tax benefit' functions, state & federal.
};

estimateView.stateIncentives = function() {  // render ALL Washington state benefits.
};

estimateView.NetMeteringIncentive = function() {  //  render WA state 'net metering' benefit.
};

estimateView.StateManufacturedIncentive = function() {  //  render WA state 'manufactured in WA' incentive.
};

estimateView.FederalIncentive = function() {  //  render federal benefits.
};
