var estimateController = {};

estimateController.incentiveBenefits = function() {
  //use this to call all other functions to calculate before going to view
};

estimateController.calcFederalBenefits = function() {
  //calculate any federal benefits
};

estimateController.calcUsageCredits = function(){
  //calculate usage credits
};

estimateController.calcStateBenefits = function() {
  //calculate any WA specific credits if in WA
};

estimateController.timeToPayOff = function() {
  //calculate how much time it will take to pay off solar panels

};
